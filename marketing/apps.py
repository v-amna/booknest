"""AppConfig for marketing app."""

from django.apps import AppConfig


class MarketingConfig(AppConfig):
    """AppConfig for marketing app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "marketing"
